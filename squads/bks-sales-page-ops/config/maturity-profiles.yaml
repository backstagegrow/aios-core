profiles:
  starter:
    description: "Fast launch with lean scope"
    constraints:
      sections_max: 7
      animation_intensity: low
      integrations: basic
  growth:
    description: "Balanced quality and speed"
    constraints:
      sections_max: 10
      animation_intensity: medium
      integrations: standard
  scale:
    description: "High maturity with robust instrumentation"
    constraints:
      sections_max: 12
      animation_intensity: medium
      integrations: advanced
      requires_finance_model: true
      requires_benchmark_reference: true
